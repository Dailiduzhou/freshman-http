# 26年木犀团队后端组第二轮招新直播仓库

在这里，你可以看到直播所用的所有代码，`markdown`文档。

## 目录

- [`docs/HTTP.md`](docs/HTTP.md)：`HTTP` 概念、状态码、请求方法、请求与响应报文、`Cookie` / `Session` / `Token`、`net/http` 三件套，以及动手练习
- [`src/naive-get`](src/naive-get)、[`src/naive-post`](src/naive-post)：用最小的代码发出 `GET` / `POST` 请求
- [`src/usersync`](src/usersync)：同样的事情，但按真实工程的方式写（分层、重试、超时、结构化日志、并发限流、退出码、测试）

